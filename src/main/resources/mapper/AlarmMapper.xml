<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.liiot.snsserver.mapper.AlarmMapper">
    <insert id="insertAlarm" parameterType="me.liiot.snsserver.model.alarm.AlarmInfo">
        INSERT INTO alarm(userId, targetId, type)
        VALUES(#{userId}, #{targetId}, #{type})
    </insert>
</mapper>