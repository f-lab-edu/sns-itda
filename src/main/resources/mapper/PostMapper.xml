<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.liiot.snsserver.mapper.PostMapper">
    <insert id="insertPost" parameterType="me.liiot.snsserver.model.post.PostUploadInfo">
        INSERT INTO post(userId, content)
        VALUES(#{userId}, #{content})
    </insert>

    <select id="getLatestPostId" parameterType="String" resultType="int">
        SELECT id FROM post
        WHERE userId = #{userId}
        ORDER BY createTime DESC LIMIT 1
    </select>

    <select id="getPost" parameterType="int" resultType="me.liiot.snsserver.model.post.Post">
        SELECT id, userId, content, createTime
        FROM post
        WHERE id = #{postId}
    </select>

    <select id="getPostsByUserId" parameterType="String" resultType="me.liiot.snsserver.model.post.Post">
        SELECT id, userId, content, createTime
        FROM post
        WHERE userId = #{userId}
        ORDER BY createTime DESC
    </select>

    <select id="isAuthorizedOnPost" resultType="boolean">
        SELECT EXISTS
        (SELECT id FROM post WHERE id = #{postId} AND userId = #{userId})
    </select>

    <update id="updatePost">
        UPDATE post
        SET content = #{content}
        WHERE id = #{postId}
    </update>

    <delete id="deletePost">
        DELETE FROM post
        WHERE id = #{postId}
    </delete>
</mapper>